// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleWKT

/// A floating point interval.
public struct Interval: Codable, Equatable, GoogleWKT._AnyPackable,
  Sendable
{
  /// The lower bound of the interval. If neither of the min fields are set, then
  /// the lower bound is negative infinity.
  ///
  /// This field must not be larger than max.
  /// Otherwise, an INVALID_ARGUMENT error is returned.
  public var min: OneOf_Min? = nil

  /// The upper bound of the interval. If neither of the max fields are set, then
  /// the upper bound is positive infinity.
  ///
  /// This field must be not smaller than min.
  /// Otherwise, an INVALID_ARGUMENT error is returned.
  public var max: OneOf_Max? = nil

  @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

  /// Initialize a new instance of `Interval`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Interval().with { $0.minimum = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private struct CodingKeys: CodingKey {
    var stringValue: Swift.String
    var intValue: Swift.Int? { nil }
    init(stringValue: Swift.String) { self.stringValue = stringValue }
    init?(intValue: Swift.Int) { nil }

    static let minimum = CodingKeys(stringValue: "minimum")
    static let exclusiveMinimum = CodingKeys(stringValue: "exclusiveMinimum")
    static let maximum = CodingKeys(stringValue: "maximum")
    static let exclusiveMaximum = CodingKeys(stringValue: "exclusiveMaximum")

    static let _knownKeys: Set<Swift.String> = [
      "minimum",
      "exclusiveMinimum",
      "maximum",
      "exclusiveMaximum",
    ]
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)

    var min: OneOf_Min? = nil
    let minCheckAndSet = {
      if min != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath, debugDescription: "Multiple values set for oneof 'min'")
        )
      }
      min = $0
    }
    if let minimum = try container.decodeIfPresent(Swift.Double.self, forKey: .minimum) {
      try minCheckAndSet(.minimum(minimum))
    }
    if let exclusiveMinimum = try container.decodeIfPresent(
      Swift.Double.self, forKey: .exclusiveMinimum)
    {
      try minCheckAndSet(.exclusiveMinimum(exclusiveMinimum))
    }
    self.min = min

    var max: OneOf_Max? = nil
    let maxCheckAndSet = {
      if max != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath, debugDescription: "Multiple values set for oneof 'max'")
        )
      }
      max = $0
    }
    if let maximum = try container.decodeIfPresent(Swift.Double.self, forKey: .maximum) {
      try maxCheckAndSet(.maximum(maximum))
    }
    if let exclusiveMaximum = try container.decodeIfPresent(
      Swift.Double.self, forKey: .exclusiveMaximum)
    {
      try maxCheckAndSet(.exclusiveMaximum(exclusiveMaximum))
    }
    self.max = max
    for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
      self._unknownFields.json[key.stringValue] = try container.decode(
        GoogleWKT.Value.self, forKey: key)
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)

    if let choice = self.min {
      switch choice {
      case .minimum(let value):
        try container.encode(value, forKey: .minimum)
      case .exclusiveMinimum(let value):
        try container.encode(value, forKey: .exclusiveMinimum)
      }
    }

    if let choice = self.max {
      switch choice {
      case .maximum(let value):
        try container.encode(value, forKey: .maximum)
      case .exclusiveMaximum(let value):
        try container.encode(value, forKey: .exclusiveMaximum)
      }
    }
    for (key, value) in self._unknownFields.json {
      try container.encode(value, forKey: CodingKeys(stringValue: key))
    }
  }

  /// The lower bound of the interval. If neither of the min fields are set, then
  /// the lower bound is negative infinity.
  ///
  /// This field must not be larger than max.
  /// Otherwise, an INVALID_ARGUMENT error is returned.
  public enum OneOf_Min: Codable, Equatable, Sendable {
    /// Inclusive lower bound.
    case minimum(Swift.Double)
    /// Exclusive lower bound.
    case exclusiveMinimum(Swift.Double)
  }

  /// The upper bound of the interval. If neither of the max fields are set, then
  /// the upper bound is positive infinity.
  ///
  /// This field must be not smaller than min.
  /// Otherwise, an INVALID_ARGUMENT error is returned.
  public enum OneOf_Max: Codable, Equatable, Sendable {
    /// Inclusive upper bound.
    case maximum(Swift.Double)
    /// Exclusive upper bound.
    case exclusiveMaximum(Swift.Double)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.retail.v2.Interval"
  }
  public init(fromAny any: GoogleWKT.`Any`) throws {
    self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleWKT.Struct {
    return try GoogleWKT._slowAnySerialize(message: self)
  }
}
